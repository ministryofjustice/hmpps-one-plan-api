version: "3"
services:
  db:
    image: postgres:14
    networks:
      - hmpps
    container_name: one-plan-db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=one-plan
      - POSTGRES_USER=one-plan
      - POSTGRES_DB=one-plan
networks:
  hmpps:
